class_name LevelBuilder extends Node

static func build(level: Level, level_data: LevelData, area_nodes: Array[AreaNode], tile_id_map: Dictionary) -> void:
	pass # TODO something like this
	#fill_grid_from_area_nodes(level_data, area_nodes, tile_id_map)
	#level.draw_grid(level_data, level.tile_layer_tilemap)
	# Add walls, highlights etc similarly
